---
/**
 * Card — --surface, 1px hairline, 12px radius, no shadow.
 * `nested` lifts to --surface-2 for a panel inside a card or a hover row.
 */
interface Props {
  as?: 'div' | 'article' | 'li' | 'section';
  nested?: boolean;
  class?: string;
}

// Rest spread forwards the parent's scoped-style attribute; see Section.astro.
const { as: Tag = 'div', nested = false, class: className = '', ...rest } = Astro.props;
const classes = ['card', nested && 'card--nested', className].filter(Boolean).join(' ');
---

<Tag class={classes} {...rest}>
  <slot />
</Tag>
